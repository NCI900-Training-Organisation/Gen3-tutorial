Manifest Services
-----------------

A manifest is a JSON file that lists records a user may be interested in
analyzing. The ``Manifest Service`` stores a manifest in a user folder in an S3
bucket and delivers it for later use.
